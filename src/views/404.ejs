      <!-- Decorative circles -->
      <div
        class="absolute top-0 right-0 w-12 h-12 bg-purple-200/50 dark:bg-purple-800/30 rounded-full animate-ping-slow"
      ></div>
      <div
        class="absolute bottom-4 left-2 w-8 h-8 bg-primary-200/50 dark:bg-primary-800/30 rounded-full animate-ping-slow"
        style="animation-delay: 0.5s"
      ></div>
      <div
        class="absolute top-8 left-0 w-6 h-6 bg-emerald-200/50 dark:bg-emerald-800/30 rounded-full animate-ping-slow"
        style="animation-delay: 1s"
      ></div>
    </div>

    <!-- Error code -->
    <div
      class="inline-flex items-center px-4 py-1.5 rounded-full bg-primary-50 dark:bg-primary-900/30 border border-primary-100 dark:border-primary-800 mb-6"
    >
      <span
        class="text-sm font-bold text-primary-600 dark:text-primary-400 tracking-wider"
        >LỖI 404</span
      >
    </div>

    <h1
      class="text-4xl sm:text-5xl font-extrabold font-headline text-gray-800 dark:text-white mb-4"
    >
      Oops! Trang không tồn tại
    </h1>
    <p class="text-gray-400 dark:text-gray-500 leading-relaxed mb-10 text-lg">
      Trang bạn đang tìm kiếm có thể đã bị xóa, đổi tên hoặc tạm thời không khả
      dụng.
    </p>

    <div class="flex flex-col sm:flex-row gap-4 justify-center">
      <a href="/" class="group btn-primary py-3.5 px-8 text-base">
        <i class="fas fa-home mr-2 group-hover:animate-bounce"></i> Về trang chủ
      </a>
      <a
        href="/books"
        class="btn-secondary py-3.5 px-8 text-base dark:bg-gray-800 dark:text-gray-300 dark:hover:bg-gray-700"
      >
        <i class="fas fa-book mr-2"></i> Thư viện sách
      </a>
    </div>

    <!-- Fun message -->
    <p class="mt-12 text-sm text-gray-300 dark:text-gray-600">
      <i class="fas fa-lightbulb mr-1 text-yellow-400"></i>
      Mẹo: Hãy kiểm tra lại URL hoặc sử dụng thanh tìm kiếm
    </p>
  </div>
</div>

<style>
  @keyframes float {
    0%,
    100% {
      transform: translateY(0) rotate(-3deg);
    }
    50% {
      transform: translateY(-12px) rotate(3deg);
    }
  }
  @keyframes ping-slow {
    0%,
    100% {
      opacity: 0.5;
      transform: scale(1);
    }
    50% {
      opacity: 0.2;
      transform: scale(1.3);
    }
  }
  .animate-float {
    animation: float 4s ease-in-out infinite;
  }
  .animate-ping-slow {
    animation: ping-slow 3s ease-in-out infinite;
  }
</style>

<%- include('partials/footer') %>
