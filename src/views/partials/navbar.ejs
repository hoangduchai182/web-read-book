<nav class="fixed top-0 w-full z-50 bg-white/80 backdrop-blur-xl shadow-[0_4px_32px_rgba(25,28,30,0.06)] border-b border-gray-100/50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16 items-center">
            <!-- Logo -->
            <a href="/" class="flex items-center gap-2.5 group">
                <div class="w-9 h-9 rounded-xl bg-gradient-to-br from-primary-600 to-primary-400 flex items-center justify-center shadow-glow group-hover:scale-110 transition-transform duration-300">
                    <i class="fas fa-book-open text-white text-sm"></i>
                </div>
                <span class="text-xl font-extrabold tracking-tight font-headline text-primary-700">BookRead</span>
            </a>

            <!-- Navigation Links -->
            <div class="hidden md:flex items-center gap-1">
                <a href="/" class="px-4 py-2 text-sm font-medium text-gray-600 hover:text-primary-600 hover:bg-primary-50 rounded-xl transition-all duration-300">
                    <i class="fas fa-home mr-1.5"></i> Trang chủ
                </a>
                <% if (typeof user !== 'undefined' && user) { %>
                    <a href="/books" class="px-4 py-2 text-sm font-medium text-gray-600 hover:text-primary-600 hover:bg-primary-50 rounded-xl transition-all duration-300">
                        <i class="fas fa-book mr-1.5"></i> Thư viện
                    </a>
                    <% if (user.role === 'admin') { %>
                        <a href="/admin" class="px-4 py-2 text-sm font-medium text-gray-600 hover:text-primary-600 hover:bg-primary-50 rounded-xl transition-all duration-300">
                            <i class="fas fa-cog mr-1.5"></i> Quản trị
                        </a>
                    <% } %>
                    <div class="ml-4 pl-4 border-l border-gray-200 flex items-center gap-3">
                        <div class="w-8 h-8 rounded-full bg-gradient-to-br from-primary-500 to-primary-600 flex items-center justify-center">
                            <span class="text-white text-xs font-bold"><%= user.full_name.charAt(0).toUpperCase() %></span>
                        </div>
                        <span class="text-sm font-semibold text-gray-700"><%= user.full_name %></span>
                        <a href="/auth/logout" class="ml-1 px-3 py-1.5 text-xs font-semibold text-red-600 bg-red-50 hover:bg-red-100 rounded-lg transition-all duration-300">
                            <i class="fas fa-sign-out-alt mr-1"></i> Đăng xuất
                        </a>
                    </div>
                <% } else { %>
                    <div class="ml-4 pl-4 border-l border-gray-200 flex items-center gap-3">
                        <a href="/auth/login" class="px-4 py-2 text-sm font-medium text-gray-600 hover:text-primary-600 rounded-xl transition-all duration-300">
                            Đăng nhập
                        </a>
                        <a href="/auth/register" class="px-5 py-2 text-sm font-semibold text-white bg-gradient-to-r from-primary-600 to-primary-500 rounded-xl shadow-soft hover:shadow-glow hover:opacity-90 active:scale-95 transition-all duration-300">
                            Đăng ký
                        </a>
                    </div>
                <% } %>
            </div>

            <!-- Mobile menu button -->
            <div class="md:hidden flex items-center">
                <button id="mobile-menu-btn" class="w-10 h-10 flex items-center justify-center text-gray-600 hover:text-primary-600 hover:bg-primary-50 rounded-xl transition-all duration-300">
                    <i class="fas fa-bars text-lg"></i>
                </button>
            </div>
        </div>
    </div>

    <!-- Mobile Menu -->
    <div id="mobile-menu" class="hidden md:hidden bg-white/95 backdrop-blur-xl border-t border-gray-100">
        <div class="px-4 py-4 space-y-1">
            <a href="/" class="flex items-center gap-3 px-4 py-3 text-gray-600 hover:text-primary-600 hover:bg-primary-50 rounded-xl transition-all">
                <i class="fas fa-home w-5"></i> Trang chủ
            </a>
            <% if (typeof user !== 'undefined' && user) { %>
                <a href="/books" class="flex items-center gap-3 px-4 py-3 text-gray-600 hover:text-primary-600 hover:bg-primary-50 rounded-xl transition-all">
                    <i class="fas fa-book w-5"></i> Thư viện
                </a>
                <% if (user.role === 'admin') { %>
                    <a href="/admin" class="flex items-center gap-3 px-4 py-3 text-gray-600 hover:text-primary-600 hover:bg-primary-50 rounded-xl transition-all">
                        <i class="fas fa-cog w-5"></i> Quản trị
                    </a>
                <% } %>
                <div class="border-t border-gray-100 my-2"></div>
                <div class="px-4 py-3 flex items-center justify-between">
                    <div class="flex items-center gap-3">
                        <div class="w-8 h-8 rounded-full bg-gradient-to-br from-primary-500 to-primary-600 flex items-center justify-center">
                            <span class="text-white text-xs font-bold"><%= user.full_name.charAt(0).toUpperCase() %></span>
                        </div>
                        <span class="text-sm font-semibold text-gray-700"><%= user.full_name %></span>
                    </div>
                    <a href="/auth/logout" class="px-3 py-1.5 text-xs font-semibold text-red-600 bg-red-50 rounded-lg">
                        <i class="fas fa-sign-out-alt mr-1"></i> Đăng xuất
                    </a>
                </div>
            <% } else { %>
                <div class="border-t border-gray-100 my-2"></div>
                <div class="px-4 py-3 flex gap-3">
                    <a href="/auth/login" class="flex-1 text-center px-4 py-2.5 text-sm font-medium text-gray-700 bg-gray-100 hover:bg-gray-200 rounded-xl transition-all">
                        Đăng nhập
                    </a>
                    <a href="/auth/register" class="flex-1 text-center px-4 py-2.5 text-sm font-semibold text-white bg-gradient-to-r from-primary-600 to-primary-500 rounded-xl">
                        Đăng ký
                    </a>
                </div>
            <% } %>
        </div>
    </div>
</nav>
<!-- Spacer for fixed navbar -->
<div class="h-16"></div>

<script>
    document.getElementById('mobile-menu-btn')?.addEventListener('click', () => {
        document.getElementById('mobile-menu')?.classList.toggle('hidden');
    });
</script>